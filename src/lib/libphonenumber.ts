// Simplified phone number formatter - can be extended with libphonenumber-js if needed
export function formatIncompletePhoneNumber(phone: string): string {
  // Simple formatting - can be enhanced with libphonenumber-js
  return phone;
}

