'use client';

import { ReactNode } from 'react';

export function ConsentManager({ children }: { children: ReactNode }) {
  // Simplified consent manager - can be extended later with @c15t/nextjs if needed
  return <>{children}</>;
}

